# S6 考核模块 · 设计提案（待用户确认后实施）

> 状态：**规划草案**，等待用户验收 S5 并确认本方案后开始执行。
> 前置：S5 训练模块已实现（知识题+案例题、满分自动完成章节标记）。

## 一、功能目标

每个技能模块配置一套**模块考核卷**，学习者完成章节讲解与训练后可参加考核：
**在线答题 → 自动评分 → 成绩记录 → 通过/未通过判定**，考核通过视为该模块学习完成。

## 二、现有基础（可复用）

- 数据模型已就绪：`ExamPaper`（考核卷，含通过线 pass_score、时限 duration_minutes）、
  `ExamRecord`（成绩记录：score/passed/answers/submitted_at）、
  `Question.category = "exam"`（考核题）
- 招聘模块已有考核卷：15 题（含书单进阶内容），通过线 60 分，时限 15 分钟
- 前端已有：模块详情页、训练页（答题交互组件可复用）

## 三、接口设计

| 接口 | 说明 |
|---|---|
| `GET /api/modules/{code}/exam` | 获取模块考核卷信息（标题/描述/通过线/时限/题数，不含答案） |
| `GET /api/modules/{code}/exam/questions` | 获取考核题（不含答案） |
| `POST /api/exam/submit` | 提交答卷：判分 + 保存 ExamRecord + 通过判定 |
| `GET /api/exam/records?module_id=` | 考核历史成绩 |
| `GET /api/exam/latest?module_id=` | 最近一次考核结果（前端结果页用） |

## 四、关键设计决策（请用户确认）

1. **出卷方式**：
   - 方案 A（推荐）：**固定试卷**——每模块 15 题一次出完，题目稳定，便于标准化考核
   - 方案 B：**随机抽题**——从考核题库随机抽 10 题，每次不同（与摸底测试一致）
2. **限时机制**：
   - 方案 A（推荐）：**前端倒计时**，超时自动提交当前答案（后端不强制）
   - 方案 B：后端记录提交耗时，仅展示不强制
3. **重新考核**：不限次数，取**最近一次**成绩为准（方案 A，推荐）；或取**最高分**（方案 B）
4. **案例题入卷**：考核卷中是否加入【案例应用题】（与训练一致，如加入建议 15 题中含 3 道案例题）
5. **考核与章节完成的关系**：考核通过后，是否自动标记该模块**全部未完成章节**为已完成？
   - 方案 A（推荐）：不自动标记，保持学习记录真实
   - 方案 B：自动标记（模块视为整体完成）

## 五、前端页面

1. **考核入口**：模块详情页顶部"模块考核"卡片（显示通过线/题数/最近成绩状态）
2. **考核须知页** `/modules/:code/exam`：考试规则（题数/时限/通过线）+ [开始考核]
3. **答题页** `/modules/:code/exam/take`：倒计时 + 分节作答（知识题/案例题）+ 交卷确认
4. **成绩页** `/modules/:code/exam/result`：大号得分、通过/未通过判定、逐题反馈、[重新考核]/[返回模块]

## 六、与教学任务联动（S7 完善）

- 考核通过后，教学任务中该模块状态更新为"已通过 ✅"
- 未通过显示"未通过，建议复习后重考"

## 七、验收标准

- 完成"讲解 → 训练 → 考核"完整闭环
- 成绩持久化，可查看历史
- 与摸底测试/训练/进度数据互不冲突
